input {
  beats {
    port => 5044
  }
}

filter {
  grok {
    match => {
      "message" => "(?<date>%{YEAR}-%{MONTHNUM}-%{MONTHDAY}\s*%{TIME})\s*%{WORD:loglevel}\s*%{NUMBER:pid}[\s-]*[\[\s]*%{WORD:thread}[\]\s]*%{JAVACLASS:javaclass}[\s\:]*%{GREEDYDATA:log}"
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{[@metadata][beat]}-%{[@metadata][version]}" 
  }
}
